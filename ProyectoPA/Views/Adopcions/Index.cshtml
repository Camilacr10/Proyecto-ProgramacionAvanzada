@model IEnumerable<ProyectoPA.Adopcion>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.fecha_adopcion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Mascota.nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Usuario.username)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.fecha_adopcion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Mascota.nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Usuario.username)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_adopcion }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_adopcion }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_adopcion })
        </td>
    </tr>
}

</table>
