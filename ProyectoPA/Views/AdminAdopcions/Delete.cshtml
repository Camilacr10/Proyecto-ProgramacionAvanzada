@model ProyectoPA.Adopcion

@{
    ViewBag.Title = "Eliminar Adopción";
    Layout = "~/Views/AdminHome/Layout_Admin_Home.cshtml";
}

<h2 class="text-center my-4">Eliminar Adopción</h2>

<h3 class="text-danger">¿Está seguro que desea eliminar esta adopción?</h3>
<div class="card p-4 mb-4 d-flex flex-column align-items-center mx-auto" style="max-width: 800px;">
    <h4 class="mb-3 text-center">Detalles de la Adopción</h4>
    <dl class="row d-flex justify-content-center">
        <dt class="col-sm-3">
            @Html.DisplayNameFor(model => model.fecha_adopcion)
        </dt>
        <dd class="col-sm-9">
            @Html.DisplayFor(model => model.fecha_adopcion)
        </dd>

        <dt class="col-sm-3">
            @Html.DisplayNameFor(model => model.Mascota.nombre)
        </dt>
        <dd class="col-sm-9">
            @Html.DisplayFor(model => model.Mascota.nombre)
        </dd>

        <dt class="col-sm-3">
            @Html.DisplayNameFor(model => model.Usuario.username)
        </dt>
        <dd class="col-sm-9">
            @Html.DisplayFor(model => model.Usuario.username)
        </dd>
    </dl>

    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()
        <div class="form-actions text-center">
            <input type="submit" value="Eliminar" class="btn btn-danger me-3" />
            @Html.ActionLink("Volver al listado", "Index", null, new { @class = "btn btn-secondary" })
        </div>
    }
</div>
